@model IEnumerable<Datos.Model.Parqueo>

@{
    ViewData["Title"] = "Index";
}


<p>
    <a asp-action="Create">Nuevo</a>
</p>
<table id="example" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlacaVehiculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaIngreso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaSalida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PlacaVehiculo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaIngreso)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaSalida)
                </td>
                <td>
                    @if (item.Estado == 1)
                    {
                        <label>Activado</label>
                    }
                    else
                    {
                        <label>Desactivado</label>
                    }
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Codigo">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.Codigo">Detaille</a> |
                    @if (item.Estado == 1)
                    {
                        <a asp-action="Desactivar" asp-route-id="@item.Codigo">Desactivar</a>
                    }
                    else
                    {
                        <a asp-action="Activar" asp-route-id="@item.Codigo">Activar</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
@section Scripts
{
    <script type="text/javascript" src="~/js/TablaPaginacion.js"></script>
    <script>
        $(document).ready(function () {
            $('#example').dataTable();
        });
    </script>

}
